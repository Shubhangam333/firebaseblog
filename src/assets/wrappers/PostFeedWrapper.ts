import styled from "styled-components";

const PostFeedWrapper = styled.div`
  display: flex;

  .left-container {
    flex: 4;
  }
  .right-container {
    flex: 2;
  }
`;

export default PostFeedWrapper;
